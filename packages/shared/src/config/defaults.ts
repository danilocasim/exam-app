/**
 * Shared default configuration values.
 * Apps can import these and override as needed.
 */
export { EXAM_CONFIG, SYNC_CONFIG, STORAGE_CONFIG } from './app.config';
export type { ExamConfig, SyncConfig, StorageConfig } from './app.config';
